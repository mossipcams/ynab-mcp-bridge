name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    if: github.event_name != 'pull_request' || !startsWith(github.event.pull_request.head.ref, 'release-please--')
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x, 24.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test:ci

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-node-${{ matrix.node-version }}
          path: artifacts/test-results
          if-no-files-found: error

  quality:
    if: github.event_name != 'pull_request' || !startsWith(github.event.pull_request.head.ref, 'release-please--')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run coverage thresholds
        run: npm run test:coverage

      - name: Run dependency rules
        run: npm run lint:deps

      - name: Run ESLint
        run: npm run lint

      - name: Run Oxlint advisory pilot
        continue-on-error: true
        run: npm run lint:oxlint

      - name: Run TypeScript typecheck
        run: npm run typecheck

      - name: Run Knip
        run: npm run lint:unused

      - name: Run JSCPD
        run: npm run lint:duplicates

      - name: Run tech debt report
        run: npm run tech-debt:report

      - name: Build package
        run: npm run build

      - name: Verify build artifacts are committed
        run: npm run verify:build-sync

      - name: Smoke-test packed npm artifact
        run: npm run verify:pack

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-node-22.x
          path: artifacts/coverage
          if-no-files-found: error
