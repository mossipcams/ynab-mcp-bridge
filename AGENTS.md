# AGENTS.md

## PR Rules

Default PR creation to `mossipcams/ynab-mcp-bridge`.
Never open a PR against `main` unless the user explicitly asks for `main`.
Do not open PRs, create commits for, push to, or take any other action against `calebl/ynab-mcp-server` unless the user explicitly asks for that target repo.
