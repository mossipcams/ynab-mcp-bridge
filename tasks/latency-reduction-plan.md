# Latency Reduction Plan

Implementation source of truth:
see `tasks/todo.md` for the approved execution slices and TDD sequence.

This work will prioritize:

- measurable baseline first
- additive API changes before contract changes
- opt-in prose before any default-format shift
- targeted schema trimming before any tool deletion
- consolidation only if measurements still show it is necessary
